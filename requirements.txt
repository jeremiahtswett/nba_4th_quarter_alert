# Runtime is pure Python stdlib. (requests is deliberately NOT used: Akamai
# blocks urllib3's TLS fingerprint on cdn.nba.com — see alerter/nba_client.py.)
tzdata>=2024.1  # IANA timezone data for zoneinfo on platforms without it (Windows)
